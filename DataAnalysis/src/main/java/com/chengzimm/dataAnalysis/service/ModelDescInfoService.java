package com.chengzimm.dataAnalysis.service;

import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;
import com.baomidou.mybatisplus.core.conditions.update.UpdateWrapper;
import com.baomidou.mybatisplus.extension.service.IService;
import com.chengzimm.dataAnalysis.model.ModelDescInfo;
import java.util.List;

public interface ModelDescInfoService extends IService<ModelDescInfo> {

 /*   //查询所有信息
    List<ModelDescInfo> selectList();
    //增加信息
    Integer insert(ModelDescInfo modelDescInfo);
    //更新信息
    Integer updateWrapper(ModelDescInfo modelDescInfo, UpdateWrapper<ModelDescInfo> userUpdateWrapper);
    //删除信息
    Integer delete(QueryWrapper<ModelDescInfo> queryWrapper);*/
}
